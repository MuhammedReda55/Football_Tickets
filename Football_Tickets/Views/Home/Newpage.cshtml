@*<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Background Image with Countdown</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            background-image: url('/Images/skysports-premier-league.jpg'); /* ضع رابط الصورة هنا */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            position: relative;
        }

            body::before {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0, 0, 0, 0.3);
            }

        .countdown {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: white;
            font-size: 3rem;
            font-family: Arial, sans-serif;
            z-index: 1;
        }

            .countdown span {
                padding: 0 10px;
            }
    </style>
</head>
<body>
    <div class="countdown">
        <span id="days">00</span>:
        <span id="hours">00</span>:
        <span id="minutes">00</span>:
        <span id="seconds">00</span>
    </div>

    <script>
        // حساب وقت النهاية (بعد يومين من الآن)
        const countdownDate = new Date();
        countdownDate.setDate(countdownDate.getDate() + 2); // يضيف يومين للتاريخ الحالي

        // تحديث العد التنازلي كل ثانية
        const countdownFunction = setInterval(() => {
            // الحصول على الوقت الحالي
            const now = new Date().getTime();

            // حساب الوقت المتبقي
            const distance = countdownDate - now;

            // استخراج الأيام والساعات والدقائق والثواني
            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            // عرض القيم في الصفحة
            document.getElementById("days").innerText = String(days).padStart(2, "0");
            document.getElementById("hours").innerText = String(hours).padStart(2, "0");
            document.getElementById("minutes").innerText = String(minutes).padStart(2, "0");
            document.getElementById("seconds").innerText = String(seconds).padStart(2, "0");

            // إذا انتهى الوقت، عرض رسالة "انتهى الوقت"
            if (distance < 0) {
                clearInterval(countdownFunction);
                document.querySelector(".countdown").innerHTML = "EXPIRED";
            }
        }, 1000);
    </script>
</body>
</html>
*@


<!DOCTYPE html>
<html lang="en">
<head style="background-color: white;>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Background Image with Countdown</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            background-image: url('/Images/skysports-premier-league.jpg'); /* ضع رابط الصورة هنا */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            position: relative;
        }

            body::before {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0, 0, 0, 0.3);
            }

        .countdown {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: white;
            font-size: 3rem;
            font-family: Arial, sans-serif;
            z-index: 1;
            display: flex;
            gap: 20px; /* مسافة بين العناصر */
        }

        .time-box {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .label {
            font-size: 1.2rem;
            margin-bottom: 5px;
            text-transform: uppercase;
            font-weight: bold;
        }

        .value {
            font-size: 3rem;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="countdown">
        <div class="time-box shadow-text ">
            <div class="label">Days</div>
            <div class="value" id="days">00</div>
        </div>
        <div class="time-box shadow-text">
            <div class="label">Hours</div>
            <div class="value" id="hours">00</div>
        </div>
        <div class="time-box shadow-text">
            <div class="label">Minutes</div>
            <div class="value" id="minutes">00</div>
        </div>
        <div class="time-box shadow-text">
            <div class="label">Seconds</div>
            <div class="value" id="seconds">00</div>
        </div>
      
    </div>
    <div style="text-align: center; margin-top: 300px;">
        <button style="padding: 10px 20px; font-size: 1.5rem; background-color: #dc307c; color: white; border: none; border-radius: 5px; cursor: pointer;">
            Book  Ticket Now
        </button>
    </div>
   

    <script>
        // حساب وقت النهاية (بعد يومين من الآن)
        const countdownDate = new Date();
        countdownDate.setDate(countdownDate.getDate() + 2); // يضيف يومين للتاريخ الحالي

        // تحديث العد التنازلي كل ثانية
        const countdownFunction = setInterval(() => {
            // الحصول على الوقت الحالي
            const now = new Date().getTime();

            // حساب الوقت المتبقي
            const distance = countdownDate - now;

            // استخراج الأيام والساعات والدقائق والثواني
            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            // عرض القيم في الصفحة
            document.getElementById("days").innerText = String(days).padStart(2, "0");
            document.getElementById("hours").innerText = String(hours).padStart(2, "0");
            document.getElementById("minutes").innerText = String(minutes).padStart(2, "0");
            document.getElementById("seconds").innerText = String(seconds).padStart(2, "0");

            // إذا انتهى الوقت، عرض رسالة "انتهى الوقت"
            if (distance < 0) {
                clearInterval(countdownFunction);
                document.querySelector(".countdown").innerHTML = "EXPIRED";
            }
        }, 1000);

    </script>
</body>
</html>
